---
role:
  # os: ["archlinux"]
  pkg:
    - base-devel
    - ca-certificates
    - cmake
    # - firefox
    # - git
    - imagemagick
    - mlocate # Run 'updatedb'
    # - mpv # (vlc) Requires qt4
    - the_silver_searcher
    # - transmission-cli # remote/gtk/qt
    - unzip
    - youtube-dl

    # - php

    # - nodejs
    # - npm

    # - python-pip

    # - ruby
    # - ruby-bundler
  post_install:
    # $(mktemp -d);
    - "echo $DOT; ~/.dot/arch/install.sh"
